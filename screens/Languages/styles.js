import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  screen: {
    margin: 20,
  },
  language: {
    padding: 10,
    alignItems: 'center',
    borderWidth: 1,
    borderRadius: 10,
    marginBottom: 10,
  },
});

export default styles;
